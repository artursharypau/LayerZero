namespace LayerZero.Characters.Enemies.Skeleton
{
    /// <summary>
    /// A skeleton is a plain melee enemy: everything it does comes from the shared behaviour
    /// plus its own config asset, animator and prefab.
    /// </summary>
    public sealed class SkeletonController : MeleeEnemyController
    {
    }
}
